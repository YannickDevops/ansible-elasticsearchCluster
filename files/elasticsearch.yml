network.host: 0.0.0.0
#network.publish_host: "{{ ansible_ec2_public_ipv4 }}"
#discovery.zen.ping.unicast.hosts: ["{{ ansible_ec2_public_ipv4 }}"]
discovery.zen.ping.unicast.hosts: ["{{ ansible_default_ipv4.address }}"]
cluster.name: logging
# path.data: /path/to/data
# path.logs: /path/to/logs
# http.port: 9200
# path.repo: /var/backups
discovery.zen.minimum_master_nodes: 3

